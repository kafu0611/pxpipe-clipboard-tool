// Cost-model constants shaped like pxpipe-proxy's dist/core/transform.js,
// with values distinct from the adapter's fallbacks so tests can tell them apart.
export const REPORT_CHARS_PER_TOKEN = 5.5;
export const ANTHROPIC_PIXELS_PER_TOKEN = 500;
export const IMAGE_COST_SAFETY_MARGIN = 1.25;
