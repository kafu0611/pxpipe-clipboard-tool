// Minimal stand-in for pxpipe-proxy's public barrel, used by adapter tests.
export const FIXTURE_MARKER = "fake-pxpipe";
export async function renderTextToImages() {
  return { pages: [], droppedChars: 0 };
}
