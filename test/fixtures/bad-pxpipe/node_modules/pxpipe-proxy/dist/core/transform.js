// Broken cost model (non-finite / non-positive values) — the adapter must
// reject these and fall back to its pinned literals.
export const REPORT_CHARS_PER_TOKEN = Number.NaN;
export const ANTHROPIC_PIXELS_PER_TOKEN = 0;
export const IMAGE_COST_SAFETY_MARGIN = "not a number";
